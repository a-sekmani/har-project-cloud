services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cloudhar
      POSTGRES_PASSWORD: cloudhar
      POSTGRES_DB: cloudhar
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
