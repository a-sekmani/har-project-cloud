Unify UI, add SQLite support, reorganize tests and samples

Backend & database
- Add SQLite support: connect_args for SQLite, compile UUID as TEXT for SQLite
- On startup with SQLite, create tables from models (no Alembic)
- Add docker-compose.yml with PostgreSQL service (port 5433) for optional use

Tests
- Reorganize tests into unit/, api/, integration/ with shared fixtures/
- unit: test_health, test_normalize, test_infer_schema, test_database
- api: test_ingest, test_face_api, test_golden_edge, test_multiple_people
- integration: test_integration
- Fix test_golden_edge fixture path (parent.parent/fixtures)
- Update docs/development.md with new paths and test layout

Samples
- Move test_request.json, test_request_valid.json, test_request_low_conf.json into samples/
- Add samples/README.md; update test_scenarios.sh to use samples/test_request.json
- Document samples and test_scenarios.sh in development.md

Person Management
- Show "Gallery last updated" (created_at date) instead of version number on persons and person_detail pages

UI: layout and navigation
- Use max-width 1600px (Label Windows width) on all pages: dashboard, windows, label, persons, person_detail, device_dashboard
- Add shared site header on every page: logo + nav (Dashboard, Recent Windows, Label Windows, Person Management)
- Align device_dashboard styling with app theme (CSS variables, same header)

Branding
- Replace "Cloud HAR" with "HAR Cloud App" in all template headers

Label Windows table
- Add Person and Face Conf columns (match Recent Windows); show person name with link to person detail and face confidence with color coding
