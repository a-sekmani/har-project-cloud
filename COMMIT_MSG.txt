Unify filters and UI across Dashboard, Windows, and Label pages

Filters (all pages with filters)
- Standardise filter layout: 3 rows — (1) main controls, (2) Person/Device/Camera/Activity dropdowns, (3) checkboxes + actions
- Use same order everywhere: Person → Device → Camera → Activity
- Device/Camera: populate from GET /v1/dashboard/filter-options (all distinct values, no time filter so dropdowns always show options)
- Activity: populate from model labels (/v1/models/{key})
- Add .header { flex-direction: column } and .header .filter-row for consistent styling

Dashboard
- Rename title and h1 from "Overview Dashboard" to "Dashboard"

Recent Windows
- Replace Device/Camera text inputs with selects; add Activity select
- Remove Track filter and Track column
- Auto-refresh: "Auto-refresh (5s)" only (fixed 5s), move to row 3; remove Interval dropdown
- Remove stats block (Windows count, With predictions, Avg conf, etc.)
- Remove Last updated

Label Windows
- Same 3-row filter style; Device/Camera/Activity as selects
- Add Person filter (from /v1/persons); remove Track filter and Interval
- Remove bulk section: "Apply label to selected" and "Assign person to selected"
- Auto-refresh (5s) in row 3; remove Last updated

Person Detail — Recent Windows tab
- Add full filter bar: Model, Limit | Device, Camera, Activity | Only with predictions, Only low conf, Auto-refresh (5s), Refresh
- Populate Model from /v1/models; Device/Camera from filter-options; Activity from model labels

Cleanup
- Remove unused .stats CSS from windows.html; align font-size and h1 color on label/windows
